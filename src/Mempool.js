import Transaction from './Transaction'
import { Set, List } from 'immutable'

export default class Mempoll {
    transactions = Set()

    addTransaction (transaction) {
        // eslint-disable-next-line no-useless-catch
        try {

        } catch (error) {
            throw error
        }
    }
}